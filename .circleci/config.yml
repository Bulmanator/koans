version: 2.1

orbs:
  ruby: circleci/ruby@2.6

jobs:
  build:
    docker:
      - image: cimg/base:2022.10
    steps:
      - checkout
      - ruby/install:
          version: '2.6.3'
      - run: ruby -v

workflows:
  run-jobs:
    jobs:
      - build
